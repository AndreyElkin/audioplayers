# Приложение «Аудиоплеер»

## Техническое задание «Приложение „Аудиоплеер"»

### Что нужно сделать
Вам нужно реализовать клиентскую часть приложения «Аудиоплеер» в соответствии с макетом.

Для работы вам потребуется бэкенд. Разверните локально проект express-backend, установите все зависимости командой npm install (либо используя пакетный менеджер, подходящий для вашей ОС). Далее вы можете запустить сервер. Перейдите в проект, из него запустите сервер, используя команду start.

### Описание работы API

**Base URL:** http://localhost:8000  
**API Base Path:** http://localhost:8000/api

#### Endpoints

**1. Регистрация нового пользователя.**

- **URL:** `/register`
- **Method:** POST
- **Описание:** регистрирует нового пользователя. Проверяет, существует ли пользователь с таким именем, и создаёт запись с хешированным паролем.

**Пример запроса:**
```
POST /api/register HTTP/1.1
Content-Type: application/json

{
  "username": "user1",
  "password": "securepassword"
}
```

**Пример ответа (успешная регистрация):**
```json
{
  "message": "пользователь успешно добавлен",
  "user": {
    "username": "user1"
  }
}
```

**Пример ответа (ошибка):**
```json
{
  "message": "пользователь уже существует"
}
```

**2. Авторизация пользователя.**

- **URL:** `/login`
- **Method:** POST
- **Описание:** авторизует пользователя. Проверяет имя пользователя и пароль, и в случае успеха возвращает токен для работы с API.

**Пример запроса:**
```
POST /api/login HTTP/1.1
Content-Type: application/json
{
  "username": "user1",
  "password": "securepassword"
}
```

**Пример ответа (успешная авторизация):**
```json
{
  "message": "авторизация прошла успешно",
  "token": "eyJhbGciOiJIUzI1NiIsInR5..."
}
```

**Пример ответа (ошибка):**
```json
{
  "message": "произошла ошибка при авторизации — неверные данные"
}
```

**3. Получение списка композиций.**

- **URL:** `/tracks`
- **Method:** GET
- **Описание:** возвращает список всех доступных музыкальных композиций.

**Пример запроса:**
```
GET /api/tracks HTTP/1.1
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5...
```

**Пример ответа:**
```json
[
  {
    "id": "1",
    "title": "Song A",
    "artist": "Artist A"
  },
  {
    "id": "2",
    "title": "Song B",
    "artist": "Artist B"
  }
]
```

**4. Работа с избранными композициями.**

**Получение списка избранного.**
- **URL:** `/favorites`
- **Method:** GET
- **Описание:** возвращает список треков, добавленных в избранное пользователем.

**Пример запроса:**
```
GET /api/favorites HTTP/1.1
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5...
```

**Пример ответа:**
```json
[
  {
    "id": "1",
    "title": "Song A",
    "artist": "Artist A"
  }
]
```

**Добавление композиции в избранное.**
- **URL:** `/favorites`
- **Method:** POST
- **Описание:** добавляет трек в список избранного пользователя.

**Пример запроса:**
```
POST /api/favorites HTTP/1.1
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5...
Content-Type: application/json
{
  "trackId": "1"
}
```

**Пример ответа:**
```json
{
  "message": "композиция добавлена в избранное"
}
```

**Удаление композиции из избранного.**
- **URL:** `/favorites`
- **Method:** DELETE
- **Описание:** удаляет трек из списка избранного пользователя.

**Пример запроса:**
```
DELETE /api/favorites HTTP/1.1
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5...
Content-Type: application/json
{
  "trackId": "1"
}
```

**Пример ответа:**
```json
{
  "message": "композиция убрана из избранного"
}
```

#### Авторизация

Для всех защищённых эндпоинтов (`/favorites`, `/tracks`) требуется токен. После успешного логина токен возвращается в ответе. Токен должен быть передан в заголовке Authorization в формате:

```
Authorization: Bearer <token>
```

### Фундаментально вашу задачу можно разделить на следующие структурные блоки:

1. **Визуальная составляющая.** Сюда входит вёрстка компонентов приложения, их оформление, визуальные переходы между страницами, анимации.
2. **Клиентская логика.** Вам нужно обеспечить правильное взаимодействие всех компонентов приложения, оптимизировать эти процессы, используя базовые принципы разработки и архитектуры.
3. **Взаимодействие с сервером.** Здесь ваша зона ответственности находится только в пределах клиентской части. Нужно грамотно настроить логику взаимодействия с API, работу с полученными данными и их отправкой на сервер.

### Вёрстка компонентов

- Адаптивность приложения под разные виды устройств.
- Корректная работа в браузерах Chrome, Firefox, Safari, Opera, Edge (достаточно самых свежих версий).
- Валидный CSS и HTML. Для проверки можете использовать любой удобный валидатор.
- Допускается использование препроцессоров.
- Именование классов в соответствии с методологией БЭМ. Либо можете использовать какую-нибудь библиотеку компонентов — в таком случае напишите пару слов, почему выбрали именно её для вашего проекта.

### Разработка логики приложения

- Для построения модулей приложения можете пользоваться двумя подходами: создать статичные фрагменты HTML в виде строки и добавлять в них вкрапления логики на TypeScript либо генерировать все элементы на лету по мере выполнения скрипта.
- Избегайте прямого использования низкоуровневых методов работы с DOM. Выберите подходящую для этих целей библиотеку (рекомендуем Redom).
- Обязательная и осмысленная типизация на проекте. Использование any должно быть исключено.
- Логически настроенная навигация в приложении. Кнопки перехода между экранами.

### Необходимые функции приложения (медиа)

- Прослушивание аудиофайла.
- Перемотка аудиофайла в обе стороны (плюс-минус 10 секунд) с помощью клавиатуры или мыши.
- Перемотка аудиофайла непосредственно на временной шкале по клику на её участок.
- Переход к следующему файлу.
- Переход к предыдущему файлу.
- Функция паузы.
- Добавление трека в избранное.

### Необходимые функции приложения (базовое)

- Авторизация в приложении.
- Регистрация в приложении.

### Необходимые функции приложения (навигация и иное)

- Просмотр списка треков с пагинацией. Для десктопной версии можно реализовать классическую пагинацию, для мобильной — в формате ленивой подгрузки, когда новые треки отображаются по мере приближения к концу экрана.
- Карточка трека с основной информацией о нём.
- Страница профиля пользователя с базовой информацией и аватаром-заглушкой

### Советы и рекомендации

- Используйте знания, полученные на курсе, и отталкивайтесь от того базового набора технологий, который уже изучили. Однако поиск новых решений и библиотек — неотъемлемая часть работы разработчика, поэтому будет здорово, если найдёте свои инструменты под конкретные потребности.
- Как уже говорилось выше, аудиоплеер является очень популярным типом приложений, поэтому и набор инструментов для вас столь же богат. Полезно будет изучить и уже готовые примеры с открытым исходным кодом.
- Добавьте творческий подход. Важно не урезать изначальное ТЗ и следовать макетам, но ничего не мешает вам дополнить приложение фишками в плане UI, микрологикой для большей отзывчивости плеера на действия пользователя и другими находками и решениями.
- Хорошей практикой является использование так называемых линтеров — специальных плагинов, которые в фоновом режиме следят за соответствием кода общепринятым стандартам. Пример линтера, который вы можете использовать.

### Критерии оценивания

- Соответствие заявленному функционалу.
- Отсутствие логических ошибок в интерфейсе.
- Отсутствие излишних запросов к серверу.
- Соответствие макетам.
- Структура проекта, логика организации файлов и директорий (рекомендуем ознакомиться с дополнительным материалом, посвящённым организации проекта).
- Соответствие общим принципам разработки.
- Чистота и аккуратность кода (без фанатизма 🙂)

---

## React + TypeScript + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react) uses [Babel](https://babeljs.io/) (or [oxc](https://oxc.rs) when used in [rolldown-vite](https://vite.dev/guide/rolldown)) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh

## React Compiler

The React Compiler is not enabled on this template because of its impact on dev & build performances. To add it, see [this documentation](https://react.dev/learn/react-compiler/installation).

## Expanding the ESLint configuration

If you are developing a production application, we recommend updating the configuration to enable type-aware lint rules:

```js
export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      // Other configs...

      // Remove tseslint.configs.recommended and replace with this
      tseslint.configs.recommendedTypeChecked,
      // Alternatively, use this for stricter rules
      tseslint.configs.strictTypeChecked,
      // Optionally, add this for stylistic rules
      tseslint.configs.stylisticTypeChecked,

      // Other configs...
    ],
    languageOptions: {
      parserOptions: {
        project: ['./tsconfig.node.json', './tsconfig.app.json'],
        tsconfigRootDir: import.meta.dirname,
      },
      // other options...
    },
  },
])
```

You can also install [eslint-plugin-react-x](https://github.com/Rel1cx/eslint-react/tree/main/packages/plugins/eslint-plugin-react-x) and [eslint-plugin-react-dom](https://github.com/Rel1cx/eslint-react/tree/main/packages/plugins/eslint-plugin-react-dom) for React-specific lint rules:

```js
// eslint.config.js
import reactX from 'eslint-plugin-react-x'
import reactDom from 'eslint-plugin-react-dom'

export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      // Other configs...
      // Enable lint rules for React
      reactX.configs['recommended-typescript'],
      // Enable lint rules for React DOM
      reactDom.configs.recommended,
    ],
    languageOptions: {
      parserOptions: {
        project: ['./tsconfig.node.json', './tsconfig.app.json'],
        tsconfigRootDir: import.meta.dirname,
      },
      // other options...
    },
  },
])
```
